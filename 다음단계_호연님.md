# k-ticketing-notifier — 무엇을 고쳤고, 이제 무엇을 하면 되나요

작성: 2026-06-09 (상태 배지 반영해 갱신) / 대상: 호연님

---

## 1. 진짜 문제는 따로 있었습니다 (HANDOFF의 추측과 다름)

GitHub Actions "Run failed (exit 1)"의 원인은 mjff watcher 실패가 아니었습니다.
**레포에서 핵심 코드 모듈 전체가 삭제돼 있었습니다.**

- 이전 커밋 `6245576`이 `src/config.py`, `src/state.py`, `src/scheduler.py`,
  `src/notifiers/*`, `src/crawlers/webpage.py` 등을 통째로 지웠습니다.
- 게다가 `main.py`는 한 번도 만들어진 적 없는 `src/heartbeat.py`를 import 하고 있었습니다.
- 그래서 `python main.py`는 **첫 줄 import 에서 즉시 죽어서** exit 1 → 매번 실패 메일.

→ 옛 커밋 `958546e`에서 사라진 모듈들을 전부 복구하고, `heartbeat.py`는 새로 만들었습니다.

## 2. 서울야외도서관 신호 — 두 개를 함께 봅니다 (수정됨)

처음엔 제가 화면 일부만 보고 "숫자뿐"이라 단정했는데, **그건 틀렸습니다.**
다시 확인하니 카드 **우상단에 상태 배지**가 분명히 있습니다. 그래서 **두 신호를 모두** 감시합니다:

| 신호 | 내용 | 강점 |
|---|---|---|
| (A) 상태 배지 | 정원 마감 / 신청 예정 / **신청중** / 대기 신청 중 / 신청 마감 / 종료 | 의미가 정확 — '신청중'=지금 신청 가능 |
| (B) 모집 정원 숫자 | 현재/총 (예: 200/200=마감, 199/200=취소표 1자리) | 가장 민감 — 취소표가 뜬 그 순간 포착 |

- 호연님이 노리는 핵심 전이: **정원 마감·신청 예정 → 신청중**  (A가 잡음)
- 취소표(`200/200 → 199/200`)는 배지가 늦게 바뀌어도 (B)가 잡음
- 그래서 **기본은 둘 다(`alert_triggers: ["status_open","spot_freed"]`)** 로 설정했습니다.
  하나만 원하면 `config.example.yaml` 의 그 줄을 `["status_open"]` 또는 `["spot_freed"]` 로 바꾸면 됩니다.
- '신청 예정(아직 안 열림)', '신청 마감/종료(이미 끝남)'에서는 빈자리가 있어도 **오인 알림이 안 가게** 처리했습니다.

> 어떤 신호 정책이 좋은지는 채팅에서 골라주세요 (둘 다 / 신청중만 / 숫자만). 기본은 '둘 다'입니다.

## 3. 검증 완료 (네트워크 없이 시뮬레이션)

- 5개 대상 모두 매칭 ✓ (재독단 **#3 과 #4 정확히 구분**)
- 신청중 전이 → 알림 ✓ / 취소표(199/200, 배지 늦음) → 알림 ✓
- 신청 예정(0/100)·신청 마감(기간 끝) → **오인 알림 없음** ✓
- 마감→취소표 시 알림 **1회**, 반복 스팸 없음 ✓
- import 무결성 / 파이프라인 정상 종료(exit 0) ✓
- 현재 5개 대상은 전부 `정원 마감`(가득 참) 상태입니다.

---

## 4. 지금 하실 일 (순서대로)

### ① GitHub Desktop 으로 커밋 & 푸시
1. **GitHub Desktop** 앱 열기
2. 왼쪽 변경 목록에 `src/...` 여러 개 + `main.py` + `config.example.yaml` 이 보입니다
3. 아래 **Summary** 칸에 `fix: restore core modules + seoul library (status+quota)` 입력
4. **Commit to main** 클릭 → 우측 상단 **Push origin** 클릭

### ② (선택) 워크플로우 개선판 적용
지금 워크플로우도 모듈이 복구됐으니 **그대로도 잘 돕니다.** 개선판(Node24 호환·Playwright 캐시·debug 입력)을
쓰려면 `.github/workflows/` 폴더의 **`notify.NEW.yml`** 을 `notify.yml` 로 바꾸세요.
(Drive 동기화 락 때문에 제가 기존 파일을 못 덮어썼습니다. 원하시면 다음에 다시 시도할게요.)

### ③ GitHub Secret 확인 (알림이 가려면 필수)
1. 브라우저에서 레포 → 오른쪽 위 **⚙️ Settings**
2. 왼쪽 **Security** 묶음 아래 **Secrets and variables** → **Actions** 클릭
3. **`NTFY_TOPIC`** 이 등록돼 있는지 확인 (없으면 **New repository secret**)
   - 값은 ntfy 앱에서 구독한 토픽과 **똑같아야** 합니다

### ④ 알림 채널 테스트 (10초)
1. 레포 → **Actions** 탭 → 왼쪽 **ticket-notifier** → **Run workflow**
2. **force test alert** ✅ → **Run workflow** → iPhone ntfy 앱 도착 확인

### ⑤ 진짜 동작 확인 (5개 프로그램을 잘 찾는지)
1. 같은 **Run workflow** 에서 **debug** ✅ → 실행
2. run 클릭 → **Run check** 로그 펼치기
3. 이런 줄이 5개 보이면 성공:
   `[seoul_lib] ✓ '노마드리딩 ③ ...' → 상태='정원 마감' 정원=80/80`
   - `✗ 대상 미발견` 줄이 있으면 **그 줄 캡처해서 저에게** → 제목/페이지 매칭 즉시 보정

---

## 5. 참고
- ⏰ **노마드리딩 ③** 신청기간은 **오늘(6/9) 17:00 마감**. 취소표를 노린다면 오늘이 고비.
- mjff(영화제)·firefly 는 비활성 유지. 차년도엔 config 에서 `enabled: true`.
- 🚨 비밀번호·토큰은 **코드/채팅에 절대 적지 마세요.** 값은 GitHub Secret 에만.
